#include<stdio.h>
#include<math.h>
#include<string.h>
#include"netpbm.c"

Matrix square(Matrix a){
/*if(a.width != b.height){
 fprintf(stderr,"error can't multiply. b->row and a->column are different ");
 exit(1);
}*/


}

Matrix transpose(Matrix a){
Matrix b
b = createMatrix(a.width,a.height);
}

